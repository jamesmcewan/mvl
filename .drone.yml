kind: pipeline
name: default

steps:
  - name: test
    image: node:10.7.0
    commands:
      - npm install
      - npm run lint
      - CI=true npm test

  - name: build
      commands:
        - npm run build --production
  
