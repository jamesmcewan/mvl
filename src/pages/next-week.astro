---
import getMyComics from '../data/getMyComics'
import Layout from '../layouts/Layout.astro'

const week = 'next'
const { comics, attributionText } = await getMyComics(week)
---

<Layout {week} {comics} {attributionText} />
